import React from 'react';

const StudentDetail: React.FC = () => (
  <div style={{ padding: 24 }}>
    <p style={{ color: '#6B7280' }}>Détails de l'élève — accédez via la liste des élèves.</p>
  </div>
);

export default StudentDetail;
